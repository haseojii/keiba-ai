
---

## 2. `AGENTS.md` 初版

これは **Codex に常に守ってほしい repo ルール** です。

```md
# AGENTS.md

## このリポジトリの目的

このリポジトリは、競馬予測AIを段階的に育てるためのプロジェクトである。
最初から複雑な実装を目指すのではなく、再現可能なベースラインを作り、
その後に特徴量・評価・回収率・画像/動画解析へと拡張する。

## 重要な原則

- まずは小さく作る
- 変更理由を説明できるようにする
- notebooks だけにロジックを閉じ込めない
- 再現性を重視する
- 既存構造を壊さずに改善する
- 初学者にも読めるコードを優先する
- コメント、PR、返答は日本語で書く。

## 実装ルール

### ディレクトリ責務
- `src/keiba_ai/data/`: データ読み込み・保存
- `src/keiba_ai/features/`: 特徴量作成
- `src/keiba_ai/models/`: モデル学習・推論
- `src/keiba_ai/evaluation/`: 評価指標・評価処理
- `scripts/`: 実行入口
- `tests/`: テスト
- `docs/`: 設計や方針の文書

### コード方針
- なるべく関数に分ける
- ハードコードを減らす
- 定数は設定としてまとめる
- 1ファイル1責務を意識する
- コメントより命名で意味を伝える
- ただし初学者が理解しやすいように必要な説明コメントは歓迎

### notebook の扱い
- notebook は探索と可視化に使ってよい
- 重要ロジックは `src/` に移す
- notebook のみで完結する状態を避ける

### 変更時の期待
変更を行うときは、可能なら以下も合わせて行うこと。
- README または docs の更新
- 最小限の動作確認
- 必要ならテスト追加
- 実行方法の明記

### PR知見の蓄積
意味のあるPRを作成またはマージしたときは、将来の設計判断に使える要約を `logs/prs/` に残す。
要約には以下を含めること。
- 何を変えたか
- なぜ変えたか
- 重要な設計判断
- 学んだこと
- 次に取りうるステップ

## モデル開発方針

- まずはベースラインを壊さずに改善する
- 予測精度と回収率は分けて考える
- データリークに注意する
- 新しい特徴量やモデルを追加するときは、何が改善仮説かを明確にする
- いきなり動画モデルに飛ばず、表データで開発サイクルを固める

## Codex への期待

Codex は以下を重視すること。
- いきなり大規模リファクタしない
- 既存構造を読んでから修正する
- 変更理由を短く説明する
- 必要なら複数案を提示する
- 実装だけでなく設計意図も明示する
- 初学者向けにわかりやすく説明する

## レビュー観点

コード提案時は、必要に応じて以下を自己点検すること。
- 責務分離は適切か
- 命名は明確か
- 将来の拡張に耐えるか
- notebook 依存が強すぎないか
- テストまたは動作確認方法があるか
- 今の段階に対して過剰設計ではないか

## 実行環境ルール（運用）

- GitHub関連操作（PR作成・更新・確認）は `gh` を優先して使う。
- `gh` が PATH に無い場合は `C:\Program Files\GitHub CLI\gh.exe` を使う。
- Python実行は `python` コマンドを試し、失敗時は `C:\Users\th716\AppData\Local\Programs\Python\Python313\python.exe` を使う。
- 動作確認やテスト実行時は、可能な限り上記Python実行ファイルで再現確認する。
